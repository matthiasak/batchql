{
  "name": "batchql",
  "version": "1.0.9",
  "description": "Don't just graphql, batchql.",
  "scripts": {
    "prestart": "rimraf build/*",
    "start": "tsc -w src/batchql.ts --outDir build -d",
    "prebuild": "rimraf build/*",
    "build": "NODE_ENV=production tsc src/batchql.ts --outDir build -d; echo \"done\";",
    "test": "node fuse.js test",
    "push": "npm run build; git commit -am \"snapshot\"; npm version patch; git push origin master; npm publish;"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com/matthiasak/clan"
  },
  "keywords": [
    "graphql",
    "mux",
    "multiplex"
  ],
  "main": "build/batchql.js",
  "author": "Matt Keas (@matthiasak)",
  "license": "MIT",
  "dependencies": {
    "clan-fp": "^1.1.0",
    "lodash.debounce": "^4.0.8"
  },
  "devDependencies": {
    "fuse-box": "^2.2.31",
    "fuse-test-runner": "^1.0.14",
    "rimraf": "^2.6.1",
    "typescript": "^2.5.2"
  }
}
