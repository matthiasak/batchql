{
  "name": "batchql",
  "version": "1.0.0",
  "description": "Don't just graphql, batchql.",
  "scripts": {
    "prestart": "rimraf build/*",
    "start": "tsc -w src/batchql.ts --outDir build -d",
    "prebuild": "rimraf build/*",
    "build": "NODE_ENV=production tsc src/index.ts --outDir build -d",
    "test": "node fuse.js test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com/matthiasak/clan"
  },
  "keywords": [
    "graphql",
    "mux",
    "multiplex"
  ],
  "main": "build/index.js",
  "author": "Matt Keas (@matthiasak)",
  "license": "MIT",
  "dependencies": {
    "clan-fp": "^1.0.2",
    "fuse-box": "^2.2.2",
    "fuse-test-runner": "^1.0.13",
    "lodash.debounce": "^4.0.8"
  },
  "devDependencies": {
    "rimraf": "^2.6.1",
    "typescript": "^2.4.2"
  }
}
